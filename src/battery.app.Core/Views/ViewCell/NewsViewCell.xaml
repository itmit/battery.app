<?xml version="1.0" encoding="UTF-8"?>
<ViewCell xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="battery.app.Core.Views.ViewCell.NewsViewCell">
    <ViewCell.View>
        <!--Ячейка для списка новостей-->
        <Frame Style="{StaticResource MainFrame}" Margin="0,5" HasShadow="False">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" Source="{Binding ImageSource}" VerticalOptions="Center" Aspect="AspectFill"/>
                    <StackLayout Grid.Column="2" VerticalOptions="Center" Margin="20">
                        <Label Text="{Binding Head}" Style="{StaticResource Title}" LineBreakMode="TailTruncation" MaxLines="4"/>
                        <Label Text="{Binding CreatedAt}" Style="{StaticResource Date}"/>
                    </StackLayout>
                </Grid>
            </Frame>
    </ViewCell.View>
</ViewCell>